/**
 * Central configuration for the mod.
 * All magic numbers live here — nothing is hardcoded in logic files.
 */

// SETUP: Replace "bigdoors:" with your mod's namespace prefix

// Block identifiers
export const EXAMPLE_BLOCK_ID = "bigdoors:example_block"; // SETUP: Replace with your block IDs

// Persistence
export const PERSISTENCE_KEY = "bigdoors:state"; // SETUP: Replace "bigdoors" with your mod ID

// Tick intervals (20 ticks = 1 second)
export const EXAMPLE_INTERVAL = 1200; // 60 seconds
